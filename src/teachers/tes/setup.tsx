/* eslint-disable prettier/prettier */

import { afterEach } from "vitest";
import "@testing-library/jest-dom/vitest";

// אין צורך ב-cleanup כי Vitest מנקה את ה-DOM אוטומטית אחרי כל טסט
afterEach(() => {
    // קוד לניקוי או הגדרות אחרות אחרי כל טסט
});
